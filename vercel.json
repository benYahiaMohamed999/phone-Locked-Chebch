{
    "buildCommand": "npm run build",
    "outputDirectory": "build/web",
    "installCommand": "npm install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}